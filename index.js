const getTitle = require('./functions')
const getDescription = require('./functions')

// Main function that initializes the program by generating information prompts.
const startGenerator = () => {
  // Get the title and start the generator.
  getTitle()
}

// Start the README generator.
startGenerator()